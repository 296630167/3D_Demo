using UnityEngine;

public abstract class 面板基类 : 基
{
    protected override void 启动时()
    {
        base.启动时();
    }
    //public override void 显示() => rt.anchoredPosition = Vector2.zero;
    //public override void 隐藏() => rt.anchoredPosition = new Vector2(30000, 30000);
}